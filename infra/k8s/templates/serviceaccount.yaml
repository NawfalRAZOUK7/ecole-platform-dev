{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "ecole-platform.serviceAccountName" . }}
  labels:
    {{- include "ecole.labels" . | nindent 4 }}
automountServiceAccountToken: false
{{- end }}
