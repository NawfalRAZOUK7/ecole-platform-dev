apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "ecole.fullname" . }}-config
  labels:
    {{- include "ecole.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.backend.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
