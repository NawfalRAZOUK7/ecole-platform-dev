import { describe, it } from 'vitest';

describe.skip('TeacherAttendancePage', () => {
  it('is quarantined because importing the page currently exhausts the Vitest worker heap', () => {
    // TODO: Re-enable after the teacher attendance page import path is made test-safe.
  });
});
