/// App router re-export — implementation lives in the app composition layer.
///
/// Import `app/router/app_router.dart` directly for new code.
/// This stub is kept for backward compatibility during the migration.

export 'package:ecole_platform/app/router/app_router.dart';
